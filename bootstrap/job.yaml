apiVersion: batch/v1
kind: Job
metadata:
  name: k3sup-job
spec:
  template:
    spec:
      containers:
      - name: k3sup
        image: ubuntu:latest
        command: ["/bin/sh"]
        args: ["-c", "apt-get update && apt-get install -y curl && bash /scripts/k3sup-wrapper.sh"]
        volumeMounts:
        - name: script
          mountPath: /scripts
      restartPolicy: OnFailure
      volumes:
      - name: script
        configMap:
          name: k3sup-wrapper
          defaultMode: 0744